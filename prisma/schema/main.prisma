generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["prismaSchemaFolder", "relationJoins"]
}

datasource db {
  provider = "postgresql"
  url      = env("SUBA")

  directUrl = env("SUB_DIRECT")
}
